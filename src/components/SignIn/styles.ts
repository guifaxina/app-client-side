import styled from 'styled-components'

export const Container = styled.span`
  p {
      margin-top: 1rem;
      font-size: 1.1rem;
      color: gray;

      span {
        color: var(--light-blue);
        font-weight: 500;
        cursor: pointer;
      }
    }
`;